# Secret de signature des sessions. 32 caracteres minimum.
# Generer avec : openssl rand -hex 32
SESSION_SECRET=

# Emplacement de la base SQLite. Dans le conteneur, pointer vers un volume
# pour que les comptes survivent a une reconstruction de l'image.
DATABASE_PATH=./data/mlbb.db

# URL publique, utilisee par les liens canoniques, le plan du site et le flux
# RSS. Sans slash final.
NEXT_PUBLIC_SITE_URL=https://mlbb.leoderoin.fr
